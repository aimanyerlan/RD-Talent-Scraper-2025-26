# R&D Talent Scraper — пайдаланушы нұсқаулығы

Бұл нұсқаулық веб-қосымшаны соңғы пайдаланушы ретінде қалай қолдану керектігін сипаттайды. Әкімшілік және әзірлеуші мәліметтері негізгі `README.md` файлында.

## Тіркелу және кіру

1. **Тіркелу (Register)** — толық аты-жөніңізді, электрондық поштаны және құпия сөзді енгізіңіз. Екі рет құпия сөз бірдей болуы керек.
2. Тіркелгеннен кейін тіркелгіге **растау хаты** жіберіледі (пошта бапталған болса). Хаттағы сілтемеге өтіп, аккаунтты белсендіріңіз.
3. **Кіру (Log in)** — расталған пошта мен құпия сөзбен кіріңіз. Google арқылы кіру мүмкіндігі орнатылған болса, тиісті батырманы қолданыңыз.
4. **Шығу (Logout)** — жоғарғы мәзірдегі профиль батырмасын басып, **Logout** таңдаңыз. Сессия токендері құрылғыңыздан өшіріледі.

Құпия сөзді ұмытқан жағдайда **Forgot password?** сілтемесі арқылы қалпына келтіру сұрауыңызды жіберіңіз.

## Басты бет және вакансиялар

- **Home** — жоба туралы қысқаша ақпарат.
- **Vacancies** — жиналған вакансиялар тізімі:
  - мәтін бойынша **іздеу**;
  - **орын**, **дағды**, басқа сүзгілер (бетте қолжетімді фильтрлер);
  - **сұрыптау** (мысалы, жариялану күні бойынша);
  - **пагинация** — беттер арасында өту.
- Вакансия картуна басып, **толық сипаттама**, дағдылар және сыртқы сайтқа сілтемені ашыңыз.

## Watchlist (бақылау тізімі)

Кірген пайдаланушы вакансияларды **бақылау тізіміне** қоса алады (немесе алып тастайды). Тізім **Watchlist** бөлімінде көрінеді. Бұл тек сіздің аккаунтыңызға байланысты; басқа пайдаланушылардың тізімін көре алмайсыз.

## Профиль

**Profile** бөлімінде аты-жөн, пошта және басқа өрістерді өзгертуге болады (нақты өрістер интерфейске байланысты). Құпия сөзді өзгерту үшін **Change password** бетін қолданыңыз.

## Әкімші рөлі

Әкімші аккаунты бар пайдаланушы:

- API арқылы вакансиялар мен дағдыларды **қосу, өзгерту, жою** жасай алады;
- кәдімгі пайдаланушы сияқты барлық оқу функцияларын пайдаланады.

Контентті кең көлемде басқару үшін **Django Admin** панелі де қолданылады (әкімшілерге URL мен құқық берілген жағдайда).

## API құжаттамасы

REST endpoint-тердің толық тізімі интерактивті түрде:

- Swagger UI: `/api/docs/swagger/` (мысалы, `http://localhost:8000/api/docs/swagger/`)
- ReDoc: `/api/docs/redoc/`
- OpenAPI схемасы: `/api/schema/`

Swagger-да **Authorize** батырмасы арқылы JWT `access` токенін енгізіп, қорғалған әдістерді сынауға болады.

## Мобильді құрылғы

Интерфейс кіші экрандарға бейімделген: мәзір элементтері мен тізімдер телефонда оқуға ыңғайлы болатындай орналасады.

## Қолдау көрсету

Техникалық орнату, Docker, мониторинг және әзірлеу процесі үшін жоба түбіндегі `README.md` файлын қараңыз.
